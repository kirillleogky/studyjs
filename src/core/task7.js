export default function compare(first, second) {

    return (first > second) ? first : second;

}


function exponentiation(first, second) {

  if (second < 0) return "Invalid Value";
  return first ** second;

}
